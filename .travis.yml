language: cpp

sudo: require

os:
  - linux

compiler:
  - gcc
  
#addons:
#  apt:
#    packages:
#      - libhdf5-serial-dev
#      - cmake
#      - liblapack-dev
      
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install --force-yes make premake4 liblapack-dev cmake libhdf5-serial-dev
  - wget http://sourceforge.net/projects/arma/files/armadillo-6.300.2.tar.gz
  - tar -xzvf armadillo-6.300.2.tar.gz
  - mkdir arma
  - cd armadillo-6.300.2 && cmake . && make && make install DESTDIR=../arma
  - cd ..
  - ls
  
  
before_script: true

script:
  - cd travis-test-tracer
  - premake4 gmake
  - make config=release
 